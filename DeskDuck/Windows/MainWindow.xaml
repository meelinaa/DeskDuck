<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DeskDuck.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DeskDuck"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DeskDuck">


    <Grid Background="Transparent" DataContext="{x:Bind ViewModel}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <!-- Text-Sprechblase über der Ente -->
            <Border x:Name="SpeechBubbleBorder"
                    Background="White"
                    CornerRadius="8"
                    Padding="8,6"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,-40,0,0"
                    MinWidth="80"
                    MaxWidth="200"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Visibility="{Binding NotificationVisibility, Mode=OneWay}">
                <StackPanel Spacing="2">
                    <TextBlock x:Name="SpeechBubbleTitle"
                               Text="{Binding NotificationTitle, Mode=OneWay}"
                               Visibility="{Binding TitleVisibility, Mode=OneWay}"
                               FontSize="12"
                               FontWeight="Bold"
                               Foreground="{Binding NotificationTextBrush, Mode=OneWay}"
                               TextWrapping="Wrap"/>
                    <TextBlock x:Name="SpeechBubbleMessage"
                               Text="{Binding NotificationMessage, Mode=OneWay}"
                               FontSize="12"
                               Foreground="Black"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            <Image
                x:Name="DuckImage"
                Height="50"
                Width="50"
                Stretch="Uniform"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Source="{Binding DuckImageUri, Mode=OneWay}"
                PointerPressed="DuckImage_PointerPressed"
                PointerMoved="DuckImage_PointerMoved"
                PointerReleased="DuckImage_PointerReleased">
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout x:Name="DuckContextMenu" Closed="DuckContextMenu_Closed">
                        <MenuFlyoutItem Text="Chat mit KI" Click="ChatWithAI_Click" />
                        <MenuFlyoutItem Text="Einstellungen" Click="Settings_Click" />
                    </MenuFlyout>
                </FlyoutBase.AttachedFlyout>
            </Image>
            <!-- Koordinaten-Anzeige unter der Ente -->
            <TextBlock
                Text="{Binding CoordinatesText, Mode=OneWay}"
                FontSize="10"
                Foreground="Black"
                HorizontalAlignment="Center"
                Margin="0,4,0,0"/>
        </StackPanel>
    </Grid>
</Window>
